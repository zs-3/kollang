use std.math
fn main()
    mut n = 2
    mut count = 0
    while n <= 50
        if is_prime(n)
            print(n)
            count = count + 1
        end
        n = n + 1
    end
    print("Found {count} primes")
end
